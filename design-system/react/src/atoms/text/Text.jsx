import React from 'react';
import PropTypes from 'prop-types';


export const Text = ({ label, onClick, type }) => {
  return (
    <span
     
    >  
    ciao
    </span>
  );
};

Text.propTypes = {
  /**
   * Button contents
   */

};

Text.defaultProps = {
  onClick: undefined,
  type: 'text',
  
};
